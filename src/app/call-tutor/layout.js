export const metadata = {
  title: "Call Tutor | Ask My Notes",
  description: "Full-screen AI voice tutor — speak your doubt, get it explained instantly.",
};

export default function CallTutorLayout({ children }) {
  return children;
}
