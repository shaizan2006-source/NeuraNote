// Sage — the AI Q&A experience (renamed from /ask-ai, redesign Stage 4).
// The page itself is a client component, so the title lives here.

export const metadata = {
  title: { absolute: "Sage — Ask My Notes" },
  description: "Ask Sage — your notes that answer back.",
};

export default function SageLayout({ children }) {
  return children;
}
