import RouteSkeleton from "@/components/shared/RouteSkeleton";

// Sage has a left conversation rail — use the sidebar skeleton variant.
export default function Loading() {
  return <RouteSkeleton sidebar />;
}
