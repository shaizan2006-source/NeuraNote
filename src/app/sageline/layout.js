export const metadata = {
  title: "SageLine | Ask My Notes",
  description: "SageLine picks up the phone and actually teaches you — a voice-first, exam-aware, Socratic tutor.",
};

export default function SageLineLayout({ children }) {
  return children;
}
